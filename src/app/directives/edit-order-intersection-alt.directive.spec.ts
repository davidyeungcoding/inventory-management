import { EditOrderIntersectionAltDirective } from './edit-order-intersection-alt.directive';

describe('EditOrderIntersectionAltDirective', () => {
  it('should create an instance', () => {
    const directive = new EditOrderIntersectionAltDirective();
    expect(directive).toBeTruthy();
  });
});
