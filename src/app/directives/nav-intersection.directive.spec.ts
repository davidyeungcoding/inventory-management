import { NavIntersectionDirective } from './nav-intersection.directive';

describe('NavIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
