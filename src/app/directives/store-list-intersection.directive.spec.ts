import { StoreListIntersectionDirective } from './store-list-intersection.directive';

describe('StoreListIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new StoreListIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
