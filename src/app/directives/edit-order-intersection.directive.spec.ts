import { EditOrderIntersectionDirective } from './edit-order-intersection.directive';

describe('EditOrderIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new EditOrderIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
